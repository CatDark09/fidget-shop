name = "fidget-piac-web"
pages_build_output_dir = "./"
compatibility_date = "2024-02-05"

[vars]
# Placeholder for local development. 
# REAL SECRETS MUST BE SET IN CLOUDFLARE DASHBOARD, NOT HERE.
# But for local dev with `wrangler pages dev`, we can read from .dev.vars (gitignored) or set here if not secret-secret.
# For API keys, it's best to use .dev.vars locally.
